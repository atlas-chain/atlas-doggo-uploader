node_modules/
.env
.env.*
*.local
# tooling
.claude-flow/
.playwright-mcp/
# generated artifacts
out/
downloaded*.png
*.tmp

# scratch screenshots at repo root (docs/ images stay tracked)
/*.png
# local scratch notes (contains throwaway keys) — never commit
notes.md
